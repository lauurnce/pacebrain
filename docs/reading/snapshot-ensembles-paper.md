# Snapshot Ensembles — Huang et al., 2017

Free ensembles from cyclic learning rates by saving minima snapshots.

The idea I keep coming back to: the LR schedule itself can manufacture diversity without extra training runs.

Where it touches PaceBrain: cheap way to upgrade a single GRU into an ensemble under a fixed compute budget.
