# Algorithms for Hyper-Parameter Optimization — Bergstra et al., 2011

Sequential model-based optimisation (TPE) over conditional spaces.

The idea I keep coming back to: random search is the baseline TPE quietly beats by reusing history.

Where it touches PaceBrain: hyperparameter search logs belong in experiment tracking, not shell history.
