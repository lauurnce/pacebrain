# Depth versus width

Deep narrow networks can represent some functions exponentially more compactly than shallow wide ones. Depth buys compositional structure, which is why it wins on data that is itself compositional and matters less on flat tabular problems.
