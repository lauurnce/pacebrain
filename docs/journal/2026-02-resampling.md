# Resampling

Estimating uncertainty by repeatedly drawing from the data rather than assuming a distribution. It replaces an analytical assumption with computation, which is nearly always the better trade now.
