# GANs

A generator and a discriminator trained against each other. Notoriously unstable, because the objective is a moving target rather than a fixed loss, and mode collapse is the characteristic failure.
