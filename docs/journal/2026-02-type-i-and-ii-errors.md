# Type I and II errors

False positive and false negative. They trade against each other at a fixed sample size, so lowering the significance threshold buys fewer false alarms and more misses — which of those is worse is a domain question, never a statistical one.
