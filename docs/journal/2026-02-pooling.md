# Pooling

Downsampling a feature map by taking the maximum or mean over a window. It buys translation tolerance and reduces resolution, and much recent work replaces it with strided convolutions instead.
