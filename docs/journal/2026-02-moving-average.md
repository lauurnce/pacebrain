# Moving averages

Smoothing by averaging over a trailing window. Trailing rather than centred matters for anything predictive: a centred window uses future values and is a clean way to leak without noticing.
