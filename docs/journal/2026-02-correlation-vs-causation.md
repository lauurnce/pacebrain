# Correlation and causation

Association constrains the set of possible causal stories without selecting one. The useful reframing is that causal claims require assumptions the data cannot supply, so the argument should be about the assumptions rather than the coefficient.
