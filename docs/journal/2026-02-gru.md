# GRU

A recurrent cell merging the LSTM's forget and input gates into one update gate and dropping the separate cell state. Roughly 25% fewer parameters at equal hidden size, and empirically comparable, so the choice is usually a budget question.
