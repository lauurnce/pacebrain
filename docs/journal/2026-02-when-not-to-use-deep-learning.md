# When not to reach for a network

On small tabular datasets, gradient-boosted trees usually win, need less tuning, and train in seconds. Networks earn their place on data with exploitable structure — sequence, image, text — and on tabular problems they are frequently the harder route to a worse result.
