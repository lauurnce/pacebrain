# Autoencoders

Training a network to reconstruct its own input through a narrow bottleneck. The bottleneck forces a compressed representation, and without one the network learns the identity and nothing else.
