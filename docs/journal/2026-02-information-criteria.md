# AIC and BIC

Model comparison scores that penalise parameter count against likelihood. BIC penalises harder and tends toward simpler models; both allow comparison without a validation split, which is their real appeal when data is scarce.
