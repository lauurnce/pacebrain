# Estimators

A rule for computing a parameter guess from a sample. Worth judging on three separate axes — bias, variance, and consistency — because an estimator can be unbiased and so noisy it is useless.
