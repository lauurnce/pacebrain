# Convolution

Sliding a small shared filter across the input. Two properties do the work: parameter sharing, so a feature detector is learned once rather than per position, and locality, so early layers only look at neighbourhoods.
