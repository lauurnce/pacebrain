# Backpropagation through time

Unrolling a recurrent network and applying the ordinary chain rule across timesteps. Cost grows with sequence length, which is why truncated versions exist and why gradient clipping is close to mandatory.
