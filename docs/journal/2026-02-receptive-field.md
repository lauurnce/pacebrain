# Receptive field

The region of input influencing one output unit. It grows with depth, which is the mechanism by which a stack of small local filters ends up seeing global structure without any single layer doing so.
