# The perceptron

A single linear unit with a threshold. Its historical importance is the limitation: it cannot represent XOR, because XOR is not linearly separable, and that observation stalled the field until hidden layers answered it.
