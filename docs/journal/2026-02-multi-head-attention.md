# Multi-head attention

Running several attention operations in parallel on projected subspaces and concatenating them. Different heads specialise on different relationships, which one head averaging over everything cannot do.
