# Weight tying

Sharing one parameter matrix between two places, commonly input embedding and output projection. It cuts parameters substantially and often improves results, because both matrices are learning the same vocabulary relationships.
