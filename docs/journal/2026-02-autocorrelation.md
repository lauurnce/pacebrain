# Autocorrelation

Correlation of a series with its own lags. Its practical consequence is that the effective sample size is far smaller than the row count, so standard errors computed as if rows were independent are badly optimistic.
