# Cost of a model

Training cost is one-time; inference cost recurs per request and usually dominates over any real deployment. That ratio is why a slightly worse model that is much cheaper to serve is frequently the correct choice.
