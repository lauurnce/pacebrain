# GPS accuracy

Consumer GPS error is a few metres per point and accumulates over turns, tree cover and tall buildings. Per-kilometre splits inherit that error, so a single fast split is often instrumentation rather than a surge.
