# Self-attention

Attention where queries, keys and values all come from the same sequence, letting every position consult every other. Its cost is quadratic in length, and its benefit is a constant path length between any two positions.
