# Distribution shift

When the data a model sees in use differs from what it was trained on. Distinct from overfitting — the model may have generalised perfectly to the old distribution, which simply stopped being the relevant one.
