# Study notes — what all of this is for

Working through this before touching a model, because the tooling makes it easy to skip. Statistics is the discipline of saying how much a conclusion is worth given how the data arrived. A model produces numbers regardless; whether those numbers mean anything is a separate question, answered by design and by honest evaluation rather than by fit.
