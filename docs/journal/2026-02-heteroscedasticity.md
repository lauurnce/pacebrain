# Heteroscedasticity

Residual variance changing across the range of a predictor. It does not bias coefficient estimates but invalidates their standard errors, so the model is roughly right while the confidence in it is wrong.
