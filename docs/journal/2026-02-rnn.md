# Recurrent networks

Applying the same transformation at each timestep while carrying a hidden state forward. The shared weights are what let one model handle any sequence length, and the long dependency chain is what makes them hard to train.
