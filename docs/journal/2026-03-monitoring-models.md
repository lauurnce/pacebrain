# Monitoring in production

Watching input distributions and prediction distributions, not just error rates — ground truth often arrives late or never. A shift in the inputs is the earliest available signal that a model has stopped being applicable.
