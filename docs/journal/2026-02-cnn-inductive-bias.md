# CNN inductive bias

Convolution assumes that useful features are local and position-independent. That assumption is what makes CNNs data-efficient on images and what makes them a poor fit for data where those properties do not hold.
