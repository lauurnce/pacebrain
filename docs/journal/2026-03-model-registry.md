# Model registry

Tracking trained models with their metrics, data and code versions. The problem it solves is knowing which artefact is deployed and what it scored, which becomes unanswerable from memory faster than expected.
