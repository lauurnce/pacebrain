# Skewness

Asymmetry in a distribution. Right-skewed data — durations, counts, incomes — is the common case, and a log transform is usually the cheapest way to make it behave for methods assuming symmetry.
