# Bayes' theorem

Inverts a conditional: given P(evidence|cause) it produces P(cause|evidence). The base rate is what people drop — a test with 99% accuracy for a condition affecting 1 in 10,000 still returns mostly false positives, and the arithmetic is not close.
