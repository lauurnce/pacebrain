# Simpson's paradox

A trend present in every subgroup can reverse when the groups are pooled. It is not a curiosity — it is the standard reason an aggregate comparison misleads, and the fix is knowing which variable was collapsed.
