# Knowledge distillation

Training a small model to match a large one's output distribution. The soft targets carry information about relative class similarity that hard labels discard, which is why the student can exceed one trained on labels alone.
