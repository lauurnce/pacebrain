# LSTM

A recurrent cell with a separate cell state and multiplicative gates deciding what to write, forget and read. The additive path through the cell state is the point — it lets gradient flow across many steps without being repeatedly multiplied down.
