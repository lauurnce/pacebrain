# Variational autoencoders

An autoencoder whose bottleneck is a distribution rather than a point, trained with a reconstruction term plus a divergence term pulling it toward a prior. The second term is what makes the latent space continuous enough to sample from.
