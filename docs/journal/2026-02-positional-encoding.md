# Positional encoding

Injecting position information into a model that has no inherent notion of order. Attention is permutation-invariant, so without this a sentence and its shuffling are the same input — the encoding is not an optimisation, it is load-bearing.
