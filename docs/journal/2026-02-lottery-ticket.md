# The lottery ticket hypothesis

That a trained network contains a small subnetwork which, retrained from the original initialisation, matches the full model. It suggests most of the parameters were scaffolding for finding the subnetwork rather than doing the work.
