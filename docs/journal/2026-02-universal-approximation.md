# Universal approximation

A single sufficiently wide hidden layer can approximate any continuous function on a bounded domain. Frequently misread as a practical claim — it says such a network exists, not that gradient descent will find it or that the width is reasonable.
