# Residual connections

Adding a layer's input to its output. It gives the gradient a path that skips the layer entirely, which is what makes very deep networks trainable — the network learns a correction rather than a full transformation.
