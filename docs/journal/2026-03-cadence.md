# Cadence and stride length

Pace is cadence times stride length, so any pace is reachable by different combinations. Deliberately raising cadence tends to reduce overstriding and impact loading, and the commonly quoted target of 180 is a population average rather than an individual optimum.
