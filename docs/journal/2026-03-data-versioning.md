# Data versioning

Recording which data a result came from. Without it a changed dataset makes an old number unreproducible and, worse, silently comparable to a new one — which is how two incompatible results end up in the same table.
