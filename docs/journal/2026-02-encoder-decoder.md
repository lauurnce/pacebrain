# Encoder-decoder

One network compresses the input to a representation, another generates the output from it. It separates understanding from production, which is what allows input and output lengths to differ.
