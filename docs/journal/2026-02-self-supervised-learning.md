# Self-supervised learning

Constructing a supervised task from unlabelled data — predicting a masked portion, say. It is how the field escaped its dependence on labels, and the pretext task's design determines what the representation ends up encoding.
