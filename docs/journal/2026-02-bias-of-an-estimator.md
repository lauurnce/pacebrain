# Estimator bias

The difference between an estimator's expected value and the truth. The sample variance divides by n-1 rather than n for exactly this reason: dividing by n underestimates, because the deviations are measured from the sample mean rather than the true one.
