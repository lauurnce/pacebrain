# Contrastive learning

Training representations by pulling related pairs together and pushing unrelated ones apart. It needs no labels, only a notion of what counts as related, which is where the domain knowledge quietly enters.
