# Expectation linear

Expectation is linear even for dependent variables: E[X+Y] = E[X] + E[Y]. Variance is not.

Averaging forecasts stays unbiased regardless of correlation; averaging variances does not.
