# Transformer

An architecture built from stacked self-attention and feed-forward blocks, with no recurrence. Dropping recurrence is what makes it parallelisable across the sequence during training, which mattered more for its adoption than any single accuracy result.
