# Underfitting

A model too constrained to capture the structure that is actually present, so training and validation loss are both poor and close together. The symmetry with overfitting is the useful part: a large train/validation gap means too much capacity, while two bad numbers sitting together means too little.
