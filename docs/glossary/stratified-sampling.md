# Stratified sampling

Splitting data so class proportions are preserved in every fold. Without it a rare class can land almost entirely in one split, which makes the resulting score a property of the split rather than of the model.
