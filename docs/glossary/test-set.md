# Test set

Data touched exactly once, at the end, to estimate performance on genuinely unseen input. Its value is entirely destroyed by looking at it more than once: a test set consulted repeatedly has quietly become a validation set, and the number it reports becomes optimistic.
