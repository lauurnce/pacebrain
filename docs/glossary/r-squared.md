# R-squared

The fraction of variance in the target explained by the model, scaled so that 1.0 is perfect and 0.0 matches predicting the mean. Negative values are possible and mean the model is worse than the mean baseline, which is more informative than it sounds.
