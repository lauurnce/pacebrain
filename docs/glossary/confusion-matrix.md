# Confusion matrix

A table of predicted against actual classes, with correct predictions on the diagonal. It is the diagnostic a single accuracy figure hides: two models with identical accuracy can make completely different mistakes, and only the off-diagonal cells say which errors are the ones that matter.

Confusion matrix interacts strongly with dataset size here; behaviours that are footnote-level on benchmarks are headline-level on a few hundred athlete-weeks.
