# Strides

Short accelerations of roughly 20 seconds at near-sprint speed, run relaxed with full recovery between them. They are neuromuscular rather than metabolic work, so they sharpen turnover without adding meaningful [[training-load]] — which is why they appear during a [[taper]] when everything else is being cut.
