# Grouped split

Splitting so that all records sharing an entity stay on one side. Without it the model can recognise the entity instead of learning the relationship, and the validation score measures memorisation.
