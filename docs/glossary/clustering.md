# Clustering

Grouping unlabelled data by similarity. Evaluation is the hard part: with no ground truth, any result looks like structure, and the burden is on showing the grouping means something outside the algorithm.

The failure mode to watch with Clustering is silent optimism: results improve on the validation set for reasons that will not survive contact with next month's data.
