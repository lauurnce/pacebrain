# Validation set

Data held out from training and used to make decisions — when to stop, which hyperparameters to keep, which model wins. Every decision made against it leaks a little information into the model, which is why a separate untouched test set exists.
