# Bagging

Training models on bootstrap resamples of the data and averaging them, reducing variance without much effect on bias. The resampling is what makes the members differ, which is the property an [[ensemble]] needs to be worth more than one model.
