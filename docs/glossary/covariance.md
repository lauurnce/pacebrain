# Covariance

How two variables move together, positive when they rise in step. Its magnitude depends on the scale of both variables, which makes it unreadable on its own and is why [[correlation]] exists.
