# Recovery run

A short, deliberately slow run the day after hard work, run easier than an [[easy-run]]. Its value is circulatory and habitual rather than a training stimulus, so any pace that makes it feel like a workout has defeated the purpose.

When reasoning about Recovery run, remember the measurement chain: sensor, summary statistic, weekly aggregate. Error compounds at each hop and ends up labelled signal.
