# `baselines.fit_riegel_scale`

```python
def fit_riegel_scale(train_df)
```

Defined in `src/pacebrain/baselines.py`. This walkthrough records what the function assumes about its inputs, what it guarantees about its outputs, and which test file pins that contract down.
