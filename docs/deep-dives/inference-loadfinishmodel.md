# `inference.load_finish_model`

```python
def load_finish_model(cfg)
```

Defined in `src/pacebrain/inference.py`. This walkthrough records what the function assumes about its inputs, what it guarantees about its outputs, and which test file pins that contract down.
