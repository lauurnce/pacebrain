# `seq_models.length_mask`

```python
def length_mask(lengths, max_len)
```

Defined in `src/pacebrain/seq_models.py`. This walkthrough records what the function assumes about its inputs, what it guarantees about its outputs, and which test file pins that contract down.
