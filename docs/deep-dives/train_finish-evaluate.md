# `train_finish.evaluate`

```python
def evaluate(model, loader, loss_fn)
```

Defined in `src/pacebrain/train_finish.py`. This walkthrough records what the function assumes about its inputs, what it guarantees about its outputs, and which test file pins that contract down.
