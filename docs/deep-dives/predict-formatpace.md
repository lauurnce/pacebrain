# `predict.format_pace`

```python
def format_pace(min_per_km)
```

Defined in `src/pacebrain/predict.py`. This walkthrough records what the function assumes about its inputs, what it guarantees about its outputs, and which test file pins that contract down.
