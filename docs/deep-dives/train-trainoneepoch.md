# `train.train_one_epoch`

```python
def train_one_epoch(model, X_train, y_train, loss_fn, optimizer)
```

Defined in `src/pacebrain/train.py`. This walkthrough records what the function assumes about its inputs, what it guarantees about its outputs, and which test file pins that contract down.
