# `eval.riegel_predict`

```python
def riegel_predict(avg_pace_min_per_km, race_distance_km, ref_distance_km)
```

Defined in `src/pacebrain/eval.py`. This walkthrough records what the function assumes about its inputs, what it guarantees about its outputs, and which test file pins that contract down.
