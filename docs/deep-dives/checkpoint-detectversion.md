# `checkpoint.detect_version`

```python
def detect_version(checkpoint)
```

Defined in `src/pacebrain/checkpoint.py`. This walkthrough records what the function assumes about its inputs, what it guarantees about its outputs, and which test file pins that contract down.
